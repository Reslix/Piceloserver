package com.scryer.endpoint.service;

/**
 * Hold methods for processing filtering
 */
public class FilterService {

    /**
     * For constructing a k-regular graph (fully connected, k-colorable)
     * If k=2m is even, put all the vertices around a circle, and join each to its m nearest neighbors on either side.
     *
     * If k=2m+1 is odd, and n is even, put the vertices on a circle, join each to its m nearest neighbors on each side, and also to the
     * vertex directly opposite.
     */
}
