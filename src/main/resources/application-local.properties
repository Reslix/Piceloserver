app.cookie_domain=localhost
app.port=8080
app.region=us-west-1
app.ddbEndpoint=http://localhost:8000
app.s3Endpoint=http://localhost:9000
app.redisEndpoint=http://localhost:6379

app.keystore.password=picelo
app.aws.access.key.id=fakestaws
app.aws.secret.access.key=fakestaws

management.endpoints.web.exposure.include=health, metrics, prometheus
management.metrics.tags.region=${app.region}
management.metrics.tags.stack=local
management.metrics.web.server.request.autotime.percentiles=.5,.9,.99,.999